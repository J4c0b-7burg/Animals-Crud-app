<!DOCTYPE html>
<html lang="en">
  <body>
    <main>
        <div>
            <a href="/animals/new"><button>Create A New Animal</button></a>
          <% for (animal of animals) { %>
  
          <article>
            <a href="/animals/<%= animal._id %>">
              <h2>
                <%= animal.name %> - <%= animal.isCute ? "Cute" : "Not Cute" %>
              </h2>
            </a>
          </article>
  
          <% } %>
        </div>
      </main>
</html>